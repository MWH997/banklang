# Evidence grades

What each example's evidence establishes. Generated by `pnpm evidence:grades`.

| Grade | Count | What it rules out |
| --- | --- | --- |
| executed | 21 | A defect that compiles: the program is run and its ledger, balances and branches are asserted on. |
| compiled | 2 | A program the target would reject. Says nothing about what it computes. |
| emitted | 0 | Nothing local compiles it; the conformance linter is what checks it. |

None of the three is IBM Enterprise COBOL.
See [docs/status-and-limits.md](../docs/status-and-limits.md).

| Example | Grade | Note |
| --- | --- | --- |
| `account-file-batch` | executed | differential: agrees with cobc, no hand-written expectation |
| `account-posting` | executed | differential: agrees with cobc, no hand-written expectation |
| `account-transfer` | executed | differential: agrees with cobc, no hand-written expectation |
| `amortisation-schedule` | executed | differential: agrees with cobc, no hand-written expectation |
| `batch-interest-accrual` | executed | differential: agrees with cobc, no hand-written expectation |
| `branch-accrual-cursor` | executed |  |
| `deadlock-retry` | executed | differential: agrees with cobc, no hand-written expectation |
| `end-of-day-settlement/extract` | executed | differential: agrees with cobc, no hand-written expectation |
| `end-of-day-settlement/post` | executed | differential: agrees with cobc, no hand-written expectation |
| `failed-open` | executed | differential: agrees with cobc, no hand-written expectation |
| `full-disk` | executed | differential: agrees with cobc, no hand-written expectation |
| `high-volume-master` | executed | differential: agrees with cobc, no hand-written expectation |
| `interest-posting-batch` | executed | differential: agrees with cobc, no hand-written expectation |
| `mq-request-reply` | executed | differential: agrees with cobc, no hand-written expectation |
| `online-enquiry` | executed |  |
| `parm-driven-batch` | executed | differential: agrees with cobc, no hand-written expectation |
| `rounding-conformance` | executed | differential: agrees with cobc, no hand-written expectation |
| `statement-generation` | executed | differential: agrees with cobc, no hand-written expectation |
| `vsam-browse` | executed | differential: agrees with cobc, no hand-written expectation |
| `withdrawal-with-recovery` | executed |  |
| `zunit-tested-posting` | executed | differential: agrees with cobc, no hand-written expectation |
| `end-of-day-settlement/report` | compiled | a LINAGE print file, whose page depth decides when AT END-OF-PAGE fires |
| `report-with-controls` | compiled | Report Writer: page fitting, control breaks and sum counters |
