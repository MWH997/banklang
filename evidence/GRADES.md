# Evidence grades

What each example's evidence establishes. Generated by `pnpm evidence:grades`.

| Grade | Count | What it rules out |
| --- | --- | --- |
| executed | 3 | A defect that compiles: the program is run and its ledger, balances and branches are asserted on. |
| compiled | 19 | A program the target would reject. Says nothing about what it computes. |
| emitted | 1 | Nothing local compiles it; the conformance linter is what checks it. |

None of the three is IBM Enterprise COBOL.
See [docs/status-and-limits.md](../docs/status-and-limits.md).

| Example | Grade | Note |
| --- | --- | --- |
| `branch-accrual-cursor` | executed |  |
| `online-enquiry` | executed |  |
| `withdrawal-with-recovery` | executed |  |
| `account-file-batch` | compiled | no test runs it against the reference runtime |
| `account-posting` | compiled | no test runs it against the reference runtime |
| `account-transfer` | compiled | no test runs it against the reference runtime |
| `amortisation-schedule` | compiled | no test runs it against the reference runtime |
| `batch-interest-accrual` | compiled | no test runs it against the reference runtime |
| `end-of-day-settlement/extract` | compiled | no test runs it against the reference runtime |
| `end-of-day-settlement/post` | compiled | no test runs it against the reference runtime |
| `end-of-day-settlement/report` | compiled | no test runs it against the reference runtime |
| `failed-open` | compiled | no test runs it against the reference runtime |
| `full-disk` | compiled | no test runs it against the reference runtime |
| `high-volume-master` | compiled | no test runs it against the reference runtime |
| `interest-posting-batch` | compiled | no test runs it against the reference runtime |
| `mq-request-reply` | compiled | no test runs it against the reference runtime |
| `parm-driven-batch` | compiled | no test runs it against the reference runtime |
| `report-with-controls` | compiled | no test runs it against the reference runtime |
| `rounding-conformance` | compiled | no test runs it against the reference runtime |
| `statement-generation` | compiled | no test runs it against the reference runtime |
| `vsam-browse` | compiled | no test runs it against the reference runtime |
| `zunit-tested-posting` | compiled | no test runs it against the reference runtime |
| `deadlock-retry` | emitted | needs db2-precompiler, which exists only on z/OS |
